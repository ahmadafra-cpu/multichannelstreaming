{
  "name": "multi-camera-live-view",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-Camera Live View — Trax streaming addin (static app + API/stream proxy + security). Runs as a long-running server (Docker/VM) or a Vercel serverless function.",
  "main": "server/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0"
  }
}
